# Copy to `.env` in the repo root if you want explicit local Docker values.
# Without a root `.env`, docker-compose.yml already defaults to these local values.

CLIENT_URL=http://localhost
CORS_ORIGINS=http://localhost,http://localhost:80,http://127.0.0.1,http://127.0.0.1:80

VITE_API_BASE_URL=http://localhost:5001
VITE_APP_ORIGIN=http://localhost
VITE_AUTH_MODE=microsoft
VITE_MS_CLIENT_ID=1ba6917a-f4c0-49ef-a189-7c57ffd79b50
VITE_MS_TENANT_ID=common
VITE_MS_REDIRECT_URI=http://localhost

POSTGRES_DB=timesheet
POSTGRES_USER=appuser
POSTGRES_PASSWORD=apppass
POSTGRES_HOST_PORT=5433
